-- Disable the bufferline plugin. See also config/options.lua where
-- ';' is mapped to list buffers with Treesitter and select one.

return {
  {
    "akinsho/bufferline.nvim",
    enabled = false,
  },
}
