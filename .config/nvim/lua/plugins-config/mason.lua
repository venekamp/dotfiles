require("mason").setup()
require("mason-lspconfig").setup()

-- NOTE: Setting up language service would ordinarily happen right here.
-- However, since lsp-zero is used, the specifics are moved to there.
